// Själva köraren som klicakas på "Starta körning" knappen i frontend.
let username = "temp";

// Varablierna i paths
let path_data = {};
let start = "";
let destination = "";
let length = 0;
// Estimated time of arrival
let eta = 0;


// Här kan du anropa för att sparar elemment så länge 
function savePath(user, data, start, destination, length, eta) {
  console.log("Saving path data for user: " + user);
};
